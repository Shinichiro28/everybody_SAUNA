<table class="table">
  <thead>
    <tr>
      <th></th>
      <th>ジャンル</th>
    </tr>
    <th></th>
    <th></th>
  </thead>
  <tbody>
    <% genres.each do |genre| %>
    <tr>
      <td></td>
      <td><%= genre.name %></td>
      <td><%= link_to "編集", edit_admin_genre_path(genre.id), class: "btn btn-sm btn-outline-success" %></td>
      <td><%= link_to '削除', admin_genre_path(genre.id), method: :delete, data: { confirm: '本当に削除しますか？' } , class: "btn btn-sm btn-outline-danger" %></td>
    </tr>
    <% end %>
  </tbody>
</table>