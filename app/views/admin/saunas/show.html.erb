<div class='container px-5 px-sm-0'>
  <div class='row'>
    <%= render 'public/saunas/show', sauna: @sauna %>
    <table>
      <tr>
　　    <th>販売ステータス</th>
　　    <td>
　　      <% if @product.is_active %>
            <span>販売中</span>
　　      <% else %>
            <span>販売停止中</span>
　　      <% end %>
　　    </td>
　　  </tr>
    </table>
    <%= link_to '編集する', edit_admin_sauna_path(@sauna), class: "btn btn-outline-success" %>
  </div>
</div>
