<%= render 'layouts/errors', obj: @comment %>
<%= form_with model: [sauna, post, comment], local: false do |f| %>
  <%= f.text_area :comment, rows: '5', cols: '40', class: "comment-textarea" %>
  <%= f.submit "送信", class: "btn btn-sm btn-outline-primary" %>
<% end %>
